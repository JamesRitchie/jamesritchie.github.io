```{=html}
<ul>
<% for (const item of items) { %>
  <li>
    <p>
    <strong><%= item.title %></strong>
    <br/>
    <% for (const [i, author] of item.authors.entries()) { %>
      <% if (i === item.authors.length - 1) {end = '.'} else {end = ','}%>
      <% if (author.equal) {end = '*' + end} %>
      <% if (author.url) { %>
        <a href="<%- author.url %>"><%=author.name%></a><%= end %>
      <% } else { %>
        <%=author.name%><%=end%>
      <% } %>
    <% } %>
    <br/>
    <% if (item.authors.some(e => e.equal)) { %>
      *Equal Contribution
      <br/>
    <% } %>
    <em><%= item.venue %></em>
    <br/>
    <% for (const link of item.links) { %>
        [<a href="<%- link.url %>"><%=link.name%></a>]
    <% } %>
    </p>
  </li>
<% } %>
</ul>
```